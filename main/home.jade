template(name='home')
  +thenavbar
  
  h1 #{AppName}
  
  +issuesView
  
  h3 新問題報告
  h4 填報前請filter看是否已有人報錯;填表時,緩急選待定,狀態選未解決;
  
  +quickForm(collection="Issues" id="IssuesReport" type='insert'  omitFields="提交日期,提交者")


template(name='issuesView')

    h3 已發現問題表
    h4 (亞信的朋友請移步Update看表)
    //-
      h4 舊表
      +reactiveTable(collection=OldIssues fields=fields)
      h4 新表
    +reactiveTable(collection=Issues fields=fields)
